<view class="container">
  <view class="logo">
    <text style="font-size: 48rpx; font-weight: bold; color: #1a73e8;">注册账号</text>
  </view>
  
  <view class="form">
    <input class="input" placeholder="请设置账号" bindinput="bindAccountInput" />
    <input class="input" placeholder="请设置密码" type="password" bindinput="bindPasswordInput" />
    <input class="input" placeholder="请确认密码" type="password" bindinput="bindConfirmPasswordInput" />
    
    <view class="user-type">
      <text style="font-size: 28rpx; margin-bottom: 10rpx; display: block;">选择账号类型</text>
      <view class="radio-group">
        <label class="radio-item">
          <radio value="1" checked="{{userType === 1}}" bindchange="bindUserTypeChange" />
          <text>学生账号</text>
        </label>
        <label class="radio-item">
          <radio value="2" checked="{{userType === 2}}" bindchange="bindUserTypeChange" />
          <text>商户账号</text>
        </label>
        <label class="radio-item">
          <radio value="3" checked="{{userType === 3}}" bindchange="bindUserTypeChange" />
          <text>社团账号</text>
        </label>
        <label class="radio-item">
          <radio value="4" checked="{{userType === 4}}" bindchange="bindUserTypeChange" />
          <text>管理员账号</text>
        </label>
      </view>
    </view>
    
    <button class="btn btn-primary" bindtap="register">注册</button>
    <navigator class="login-link" url="/pages/login/login">
      <text>已有账号？立即登录</text>
    </navigator>
  </view>
</view>